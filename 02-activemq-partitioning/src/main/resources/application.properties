spring.application.name=activemq-partitioning-demo

# Broker connection is overridden by Testcontainers in tests;
# this default is for local manual runs against a local broker.
spring.activemq.broker-url=tcp://localhost:61616
spring.activemq.user=admin
spring.activemq.password=admin
